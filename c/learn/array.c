#include <stdio.h>

int main(){

	int lista;
	int grade[3];

	grade[0] = 80;
	grade[1] = 80;

	lista = (grade[0] + grade[1]);
	printf("O resultado das duas grades é: %d\n" , lista);
	return 0;


}


